#### exp_01_dif_deg — polynomial degree selection

Degree sweep 2–19 (`PolynomialFeatures + StandardScaler + LinearRegression`,
DailyDelhiClimateTest, 80/20 split, seed 42), selecting the degree with the
best test R².

- **Best degree = 8** (matches the result recorded when the experiment was first run)
================================================================
